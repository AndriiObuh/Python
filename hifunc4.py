#  2. Піднесення до степеня (рекурсія)
# Реалізуй рекурсивну функцію для обчислення a^b (a в степені b).

# def step(b):
#     return lambda a: a ** b

# nf = step(3)
# print(nf(2))

# Функція, яку ти написав, використовує лямбда-функцію, але вона не є рекурсивною. Це просто функція, яка повертає степінь числа за допомогою анонімної функції.
# ✅ Рекурсивне піднесення до степеня
# Ось правильна рекурсивна реалізація a^b:


def power(a, b):
    if b == 0:  # База рекурсії: будь-яке число в степені 0 дорівнює 1
        return 1
    return a * power(a, b - 1)  # Рекурсивний виклик

# # Перевірка
print(power(2, 3))  # 8 (2 * 2 * 2)
print(power(5, 0))  # 1 (5^0 = 1)
print(power(3, 4))  # 81 (3 * 3 * 3 * 3)
# 🔍 Як це працює?
# База рекурсії: якщо b == 0, повертаємо 1 (за математичним правилом a^0 = 1).
# Рекурсія: a^b = a * a^(b-1), що зменшує b, поки не досягнемо 0.
# 🔹 Приклад виклику power(2, 3):


# power(2, 3) → 2 * power(2, 2)
# power(2, 2) → 2 * power(2, 1)
# power(2, 1) → 2 * power(2, 0)
# power(2, 0) → 1  (база рекурсії)
# Отримуємо: 2 * 2 * 2 = 8.

# Такий підхід працює за O(b) (лінійний час).