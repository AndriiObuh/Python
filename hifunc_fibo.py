# 🔹 6. Числа Фібоначчі (рекурсія)
# Напиши рекурсивну функцію, яка повертає n-те число Фібоначчі.
# 📌 Приклад:
# print(fibonacci(6))  # 8
# print(fibonacci(10)) # 55

# Числа Фібоначчі — це послідовність чисел, де кожне число є сумою двох попередніх. Перші два числа — 0 і 1, а наступні розраховуються за формулою:
# F(0) = 0
# F(1) = 1
# F(n) = F(n-1) + F(n-2) для всіх n > 1
# 🔹 Рекурсивна функція для чисел Фібоначчі

def fibonacci(n):
    if n <= 1:  # Базові випадки: 0 або 1
        return n
    return fibonacci(n - 1) + fibonacci(n - 2)  # Рекурсивне обчислення

# Перевірка
print(fibonacci(6))  # 8
print(fibonacci(10)) # 55
# 🏆 Як це працює?
# Якщо n дорівнює 0 або 1, то функція повертає саме число (базовий випадок).
# Для інших значень n, функція викликає себе рекурсивно для n-1 і n-2, а потім додає ці два значення.
# ⚡ Продуктивність
# Цей підхід має високу складність (O(2^n)), тому для великих значень n він може працювати дуже повільно. Якщо потрібно обчислювати багато чисел Фібоначчі, 
# краще використовувати динамічне програмування або мемоізацію.
# 🔹 Хочеш спробувати оптимізувати цю функцію за допомогою мемоізації чи іншого методу? 😊


# 🚀 Оптимізація рекурсії: мемоізація (кешування)
# У стандартному рекурсивному підході одні й ті самі значення обчислюються багато разів, що робить алгоритм повільним.
# Щоб це виправити, можна зберігати вже обчислені значення у словнику (кеші) та використовувати їх повторно.

# 🔹 Оптимізована функція Фібоначчі з мемоізацією (кешуванням)

def fibonacci_memo(n, cache={}):  
    if n in cache:  # Якщо значення вже є у кеші, повертаємо його
        return cache[n]
    if n <= 1:  # Базовий випадок
        return n
    cache[n] = fibonacci_memo(n - 1, cache) + fibonacci_memo(n - 2, cache)  # Зберігаємо в кеш
    return cache[n]

# Перевірка
print(fibonacci_memo(6))   # 8
print(fibonacci_memo(10))  # 55
print(fibonacci_memo(50))  # 12586269025 (швидко!)
# 🏆 Як це працює?
# Кеш (словник cache) зберігає вже обчислені значення.
# Якщо n вже є в кеші → повертаємо готовий результат.
# Інакше, викликаємо функцію рекурсивно та зберігаємо результат у кеші.
# Завдяки кешуванню ми уникаємо повторних обчислень, що значно прискорює програму.
# ⏳ Швидкість
# Звичайна рекурсія: O(2^n) (експоненційна складність, дуже повільно!)
# Мемоізація: O(n) (лінійна складність, дуже швидко!)
# 🔹 Є ще один спосіб — динамічне програмування (ітеративний підхід). Хочеш його розглянути? 😊

# 🚀 Оптимізація через динамічне програмування (ітеративний підхід)
# Ітеративний підхід використовує масив або дві змінні, щоб уникнути зайвих обчислень та рекурсії. Це ще швидший спосіб обчислення чисел Фібоначчі.

# 🔹 Ітеративний підхід (динамічне програмування)

def fibonacci_iter(n):
    if n <= 1:
        return n
    a, b = 0, 1  # Початкові значення F(0) і F(1)
    for _ in range(2, n + 1):
        a, b = b, a + b  # Обчислення наступного числа Фібоначчі
    return b

# Перевірка
print(fibonacci_iter(6))   # 8
print(fibonacci_iter(10))  # 55
print(fibonacci_iter(50))  # 12586269025 (миттєво!)
# 🏆 Як це працює?
# Використовуємо змінні a і b для збереження двох останніх значень.
# Ітеруємо від 2 до n, оновлюючи a і b за формулою b = a + b.
# Останнє значення b містить відповідь.