# 2️⃣ Напиши рекурсивну функцію, яка знаходить суму всіх чисел від 1 до n.

def sum_recursive(n):
    """Рекурсивна функція для обчислення суми чисел від 1 до n."""
    if n == 1:  # Базовий випадок: якщо n дорівнює 1, повертаємо 1
        return 1
    return n + sum_recursive(n - 1)  # Рекурсивний виклик

# Перевіримо функцію
print(sum_recursive(5))  # 15 (1 + 2 + 3 + 4 + 5)
print(sum_recursive(10))  # 55 (1 + 2 + ... + 10)
# 🔍 Як це працює?
# Функція викликає сама себе, зменшуючи n на 1, доки n не стане 1 (базовий випадок).

# Для n = 5 виклики відбуваються так:

# sum_recursive(5) = 5 + sum_recursive(4)
# sum_recursive(4) = 4 + sum_recursive(3)
# sum_recursive(3) = 3 + sum_recursive(2)
# sum_recursive(2) = 2 + sum_recursive(1)
# sum_recursive(1) = 1  # Базовий випадок
# А потім функція повертає значення у зворотному порядку:


# sum_recursive(2) = 2 + 1 = 3
# sum_recursive(3) = 3 + 3 = 6
# sum_recursive(4) = 4 + 6 = 10
# sum_recursive(5) = 5 + 10 = 15
# ✅ Плюси рекурсії
# ✔️ Код короткий та зрозумілий.
# ✔️ Добре підходить для математичних задач.

# ⚠️ Мінуси рекурсії
# ❌ Використовує більше пам’яті через стек викликів.
# ❌ При великих n може викликати переповнення рекурсії (RecursionError).

# Альтернативний підхід (без рекурсії, через формулу):


# def sum_formula(n):
#     return n * (n + 1) // 2  # Формула Гауса

# print(sum_formula(5))  # 15
# print(sum_formula(10))  # 55
# Формула Гауса працює швидше, бо виконується за O(1), тоді як рекурсія — O(n).