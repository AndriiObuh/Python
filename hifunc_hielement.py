# 🔹 8. Пошук найбільшого елемента у списку
# Напиши функцію, яка знаходить найбільше число у списку.
# 📌 Приклад:
# print(find_max([3, 7, 2, 8, 10, 4]))  # 10

# 🔹 Спосіб 1: Використання max()
# Python має вбудовану функцію max(), яка швидко знаходить найбільше значення.

def find_max(lst):
    return max(lst) if lst else None  # Повертаємо None, якщо список порожній

# 🔹 Перевірка
print(find_max([3, 7, 2, 8, 10, 4]))  # 10
print(find_max([-5, -2, -9, -1]))     # -1
print(find_max([]))                   # None (порожній список)
# ✅ Переваги: Швидко, ефективно, читається легко.

# 🔹 Спосіб 2: Використання циклу (for)
# Реалізуємо пошук найбільшого елемента вручну.

def find_max(lst):
    if not lst:  # Перевіряємо, чи список порожній
        return None
    max_num = lst[0]  # Припускаємо, що перший елемент найбільший
    for num in lst:
        if num > max_num:
            max_num = num
    return max_num

# 🔹 Перевірка
print(find_max([3, 7, 2, 8, 10, 4]))  # 10
print(find_max([-5, -2, -9, -1]))     # -1
print(find_max([]))                   # None
# ✅ Як працює?
# Спочатку вважаємо, що перший елемент — найбільший.
# Перебираємо всі числа у списку, якщо знаходимо більше — оновлюємо max_num.




    