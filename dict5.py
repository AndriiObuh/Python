# 🔹 4️⃣ Об'єднання двох списків без дублікатів
# Напиши функцію merge_lists(lst1, lst2), яка об'єднує два списки в один, виключаючи дублікати.
# 📌 Приклад:
# print(merge_lists([1, 2, 3], [3, 4, 5]))  
# # [1, 2, 3, 4, 5]
# ✅ Підказка: Використовуй множину (set) або цикл для перевірки.

# Ось декілька способів вирішення цієї задачі:
# ✅ 1. Використання множини set() (швидкий спосіб, але не зберігає порядок)

def merge_lists(lst1, lst2):
    return list(set(lst1) | set(lst2))  # Об'єднання множин

print(merge_lists([1, 2, 3], [3, 4, 5]))  
# [1, 2, 3, 4, 5] (порядок може змінитися)
# 🛠 Пояснення:

# set(lst1) | set(lst2) – об'єднання двох множин.
# list(...) – перетворює множину назад у список.
# ✅ 2. Використання dict.fromkeys() (зберігає порядок)

def merge_lists(lst1, lst2):
    return list(dict.fromkeys(lst1 + lst2))  # Видаляє дублі, зберігаючи порядок

print(merge_lists([1, 2, 3], [3, 4, 5]))  
# [1, 2, 3, 4, 5]
# 🛠 Пояснення:

# lst1 + lst2 – об'єднує два списки в один.
# dict.fromkeys(...) – створює словник, залишаючи тільки унікальні ключі (тобто унікальні числа у правильному порядку).
# list(...) – повертає результат у вигляді списку.
# ✅ 3. Використання циклу (ефективно для збереження порядку)

def merge_lists(lst1, lst2):
    merged = []
    for num in lst1 + lst2:
        if num not in merged:
            merged.append(num)
    return merged

print(merge_lists([1, 2, 3], [3, 4, 5]))  
# [1, 2, 3, 4, 5]
# 🛠 Пояснення:
# Ітеруємося по об'єднаному списку lst1 + lst2.
# Додаємо число в merged, якщо його там ще немає.
# 🔹 Який варіант вибрати?

# Швидкість важлива? ➝ set()
# Хочеш зберегти порядок? ➝ dict.fromkeys() або цикл