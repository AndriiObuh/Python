# 🔹 3️⃣ Фільтрація унікальних значень
# Напиши функцію unique_values(lst), яка приймає список і повертає новий список, що містить тільки унікальні значення (тобто без дублікатів).
# 📌 Приклад:
# print(unique_values([1, 2, 2, 3, 4, 4, 5]))  
# # [1, 2, 3, 4, 5]
# ✅ Підказка: Використовуй множину (set) або перевіряй наявність значення перед додаванням у новий список.
    

# def unique_values(lst):
#     return set(lst)

# print(unique_values([1, 2, 2, 3, 4, 4, 5]))

# Твоя функція unique_values(lst) повертає множину (set), а не список. Це працює, але множина не гарантує збереження порядку елементів. Якщо потрібно повернути список у вихідному порядку, краще використовувати цикл або dict.fromkeys().
# Ось правильний варіант, який зберігає порядок унікальних значень:

def unique_values(lst):
    return list(dict.fromkeys(lst))

print(unique_values([1, 2, 2, 3, 4, 4, 5]))  
# [1, 2, 3, 4, 5]
# ✅ Пояснення:

# dict.fromkeys(lst) створює словник, де ключі — елементи списку (ключі унікальні).
# list(...) перетворює цей словник назад у список, зберігаючи порядок появи елементів.
# Якщо порядок неважливий, можна використати множину:

def unique_values(lst):
    return list(set(lst))

print(unique_values([1, 2, 2, 3, 4, 4, 5]))  
# [1, 2, 3, 4, 5] (порядок може змінюватися)