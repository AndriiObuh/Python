# 1️⃣ Напиши функцію, яка приймає список чисел і повертає їх суму. Використовуй *args для можливості передавати довільну кількість аргументів.

# def summ(*args):
#     return sum(list(*args))

# print([1, 2, 5, 8, 14])

def summ(*args):
    return sum(args)

print(summ(1, 2, 5, 8, 14))  # Виведе: 30
# 📌 Пояснення виправлень:
# *args вже є кортежем, тому його можна напряму передати в sum(), без list().
# Викликаємо summ(1, 2, 5, 8, 14), передаючи числа як аргументи.
# 🔥 Альтернативний варіант з передачею списку:
# Якщо ти хочеш передавати список чисел, а не окремі аргументи, потрібно розпакувати список:

def summ(*args):
    return sum(args)

numbers = [1, 2, 5, 8, 14]
print(summ(*numbers))  # Виведе: 30
# 📌 Чому *numbers?
# *numbers розпаковує список у окремі аргументи: 1, 2, 5, 8, 14.
