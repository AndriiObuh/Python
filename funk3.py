# 3️⃣ Використовуючи лямбда-функцію, напиши програму, яка сортує список чисел у порядку зменшення.

# nums = lambda lst.sort().reverse()

# print(nums([1, 2, 5, 14, 62]))

# У твоєму коді є помилка. Давай розберемо її та виправимо!
# ❌ Помилки в коді

# nums = lambda lst.sort().reverse()  # ❌ lst.sort() і reverse() не можна використовувати в лямбда-функції таким чином

# print(nums([1, 2, 5, 14, 62]))  # ❌ nums - це функція, а не відсортований список
# 📌 Що не так?
# lst.sort() змінює список на місці і повертає None, тому його не можна використовувати в lambda.
# reverse() викликається на місці і також повертає None.
# Лямбда-функції зазвичай повертають значення, а не змінюють оригінальні об'єкти.
# ✅ Виправлений код

nums = lambda lst: sorted(lst, reverse=True)  # Використовуємо sorted() для створення нового списку

print(nums([1, 2, 5, 14, 62]))  # Виведе: [62, 14, 5, 2, 1]
# 📌 Пояснення виправлень:
# sorted(lst, reverse=True) створює новий відсортований список (на відміну від sort(), який змінює існуючий список і повертає None).
# lambda lst: sorted(lst, reverse=True) — тепер nums приймає список і повертає відсортовану копію.
# 🔥 Альтернативний варіант (без лямбда-функції)

def nums(lst):
    return sorted(lst, reverse=True)

print(nums([1, 2, 5, 14, 62]))  # Виведе: [62, 14, 5, 2, 1]
# 📌 Використання звичайної функції може бути більш читабельним у складних випадках.