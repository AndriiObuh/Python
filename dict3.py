# 2️⃣ Функція invert_dict(dct), яка міняє місцями ключі та значення у словнику.
# print(invert_dict({"a": 1, "b": 2, "c": 3}))  
# # {1: "a", 2: "b", 3: "c"}

def invert_dict(dct):
    return {value: key for key, value in dct.items()}

print(invert_dict({"a": 1, "b": 2, "c": 3}))  
# {1: "a", 2: "b", 3: "c"}
# 🔍 Пояснення коду:
# Використовуємо словникове включення (dict comprehension).
# dct.items() повертає пари (ключ, значення).
# value: key міняє місцями ключ і значення.
# 📌 Застереження:
# Щоб не втратити ключі, ми зберігаємо їх у списку для кожного унікального значення:

def invert_dict_safe(dct):
    inverted = {}
    for key, value in dct.items():
        if value in inverted:
            inverted[value].append(key)  # Додаємо ще один ключ у список
        else:
            inverted[value] = [key]  # Створюємо список із першим ключем
    return inverted

print(invert_dict_safe({"a": 1, "b": 2, "c": 1}))  
# {1: ['a', 'c'], 2: ['b']}
# 🔍 Детальне пояснення коду:
# Створюємо порожній словник inverted = {}.
# Перебираємо кожну пару ключ → значення у вхідному словнику dct.items().
# Якщо значення вже є у inverted:
# Додаємо ключ до існуючого списку.
# Якщо значення немає у inverted:
# Створюємо новий список із цим ключем.
# Повертаємо інверсований словник.
# 📌 Приклад роботи
# Вхідний словник:

# {"a": 1, "b": 2, "c": 1, "d": 3, "e": 2}
# Результат після інверсії:

# {
#     1: ["a", "c"],  # 1 відповідає двом ключам: 'a' і 'c'
#     2: ["b", "e"],  # 2 відповідає двом ключам: 'b' і 'e'
#     3: ["d"]        # 3 відповідає лише одному ключу: 'd'
# }

